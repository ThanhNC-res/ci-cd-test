/* Requires the Docker Pipeline plugin */
pipeline {
    /* agent { docker { image 'python:3.9' } }*/
    stages {
        stage('build') {
            steps {
                echo 'Hello Thanh'
            }
        }
    }
}
